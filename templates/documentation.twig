{% extends 'layout.twig' %}

{% block title %}{{ document_title }} — Silex Docs{% endblock %}

{% block content %}
<div class="docs-shell">
    <aside class="docs-sidebar">
        <div class="docs-sidebar-heading"><span>Documentation</span><a href="/{{ locale }}/">{{ locale == 'fr' ? 'Accueil' : 'Home' }}</a></div>
        <nav aria-label="Documentation">
            {% for group in navigation %}
                <div class="docs-nav-group">
                    <h2>{{ group.label }}</h2>
                    <ul>
                        {% for item in group.items %}
                            <li>
                                <a {% if item.path == current_path %}aria-current="page"{% endif %} href="/{{ locale }}/docs{{ item.path == 'README.md' ? '' : '/' ~ item.path }}">{{ item.label }}</a>
                            </li>
                        {% endfor %}
                    </ul>
                </div>
            {% endfor %}
        </nav>
    </aside>

    <div class="docs-main">
        <div class="docs-toolbar">
            <span>Silex {{ silex_version == 'development' ? 'dev' : 'v' ~ silex_version }} / Docs</span>
            <a href="{{ source_url }}">{{ locale == 'fr' ? 'Source canonique ↗' : 'Canonical source ↗' }}</a>
        </div>
        <article class="prose-silex">{{ documentation_html|raw }}</article>
    </div>
</div>
{% endblock %}
